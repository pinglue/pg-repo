"get-mongodb-collection":
#==============================

  description: "Test for route1"

  settings:
    singleHandler: true

  params:
    type: object
    properties:
      name:
        type: string
      exact:
        type: boolean        
    required: ["name"]
    additionalProperties: false 

  # basically the client object, no need for schema for this
  return:
    type: object
    additionalProperties: true      
